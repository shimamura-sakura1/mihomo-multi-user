# Mihomo 订阅自动更新配置
#
# 使用方式:
#   1. 添加订阅链接到本文件
#   2. 运行: ./update_subscription.sh
#
# 或者设置定时任务 (crontab -e):
#   0 */6 * * * cd /home/lzy/clash4linux/mihomo-multi-user && ./update_subscription.sh --auto >> logs/cron.log 2>&1

# 默认订阅链接（用于所有用户）
# SUB_URL=https://your-subscription-link-here

# 为特定用户配置订阅链接（优先级更高）
# 格式: SUB_URL_用户名=订阅链接
# SUB_URL_LZY=https://user-specific-link-here
